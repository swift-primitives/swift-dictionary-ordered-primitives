# ``Dictionary_Ordered_Primitives``

@Metadata {
    @DisplayName("Dictionary Ordered Primitives")
    @TitleHeading("Swift Primitives")
}

An insertion-ordered hash dictionary generic over its storage column. `Dictionary.Ordered` preserves the order in which keys were inserted while keeping O(1) average-case lookup, with copyability flowing from the column.

## Topics
```
